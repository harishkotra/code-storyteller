version: '3.8'
services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    volumes:
      - ./src:/usr/src/app/src
      - ./public:/usr/src/app/public